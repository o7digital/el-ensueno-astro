---
import AboutSlider from "@/components/react/AboutSlider";

interface Props {
  id?: string;
}

const { id } = Astro.props as Props;
---

<section id={id} class="py-16 sm:py-20 lg:py-28">
  <div class="max-w-[2100px] mx-auto pl-4 pr-0">
    <div class="grid gap-1 lg:grid-cols-[900px,1fr] items-center">
      <!-- Texte à gauche - largeur fixe -->
      <div class="space-y-6 pr-4">
        <h1 class="text-2xl sm:text-3xl uppercase tracking-[0.10em] text-dusk font-semibold">About Murmullo</h1>
        <h2 class="text-4xl leading-[1.05] sm:text-5xl lg:text-6xl font-serif">
          A sanctuary that whispers, between jungle and sea.
        </h2>
        <p class="text-lg leading-relaxed text-ink/80">
          Murmullo is a hidden boutique hotel nestled between the jungle and the sea, designed for travelers seeking tranquility without compromising beauty. Each suite opens onto soft light, mineral textures, and discreet service.
        </p>
        <p class="text-lg leading-relaxed text-ink/80">
          Wake up to the ocean's breath, dine on a fragrant patio, surrender to the pool as the sun slows down. Here, everything whispers: a slow rhythm, attentive cuisine, candlelit evenings. Nothing spectacular, just the feeling of being awaited.
        </p>
      </div>

      <!-- Slider à droite - prend tout l'espace restant -->
      <div class="pr-0">
        <AboutSlider client:visible />
      </div>
    </div>
  </div>
</section>
