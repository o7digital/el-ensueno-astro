---
import "@/styles/global.css";
import Footer from "@/components/astro/Footer.astro";
import Header from "@/components/react/Header";

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  preloadImages?: string[];
  structuredData?: Record<string, unknown>;
  currentLang?: string;
}

const {
  title,
  description,
  ogImage = "/images/hero/contact/hero-contact.webp",
  preloadImages = [],
  structuredData,
  currentLang = "en",
} = Astro.props as Props;

const canonical = Astro.site
  ? new URL(Astro.url.pathname, Astro.site).toString()
  : Astro.url.pathname;
const ogLocale = currentLang === "es" ? "es_MX" : "en_US";
---

<!DOCTYPE html>
<html lang={currentLang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#0f1411" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600;700&family=Sora:wght@300;400;500;600&family=WindSong:wght@400&display=swap"
      media="print"
      onload="this.media='all'"
    />
    {preloadImages.map((src) => (
      <link rel="preload" as="image" href={src} />
    ))}
    <title>{title}</title>
    <link rel="canonical" href={canonical} />

    <meta property="og:type" content="website" />
    <meta property="og:locale" content={ogLocale} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    {structuredData && (
      <script type="application/ld+json">
        {JSON.stringify(structuredData)}
      </script>
    )}
  </head>
  <body class="min-h-screen bg-sand text-ink">
    <Header client:load currentLang={currentLang} />
    <main id="contenu" class="relative isolate overflow-hidden">
      <slot />
    </main>
    <Footer currentLang={currentLang} />
  </body>
</html>
